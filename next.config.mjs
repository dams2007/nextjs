/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "i.pinimg.com",
            "images.pexels.com"
        ]
    }
};

export default nextConfig;
